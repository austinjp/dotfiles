#!/bin/bash

# Sorted ag = sag :)

if [ "${3}" ]; then
    ag -l "${1}" "${3}" | sort | xargs -d '\n' ag --color --group "${2}"
elif [ "${2}" ]; then
    ag -l "${1}" "${2}" | sort | xargs -d '\n' ag --color --group "${1}"
else
    echo "Requires 2 or 3 args." 1>&2
    exit 1
fi

