#!/bin/bash

# Sorted ag = sag :)

if [ "${3}" ]; then
    ag --color --group --filename "${2}" $(ag -l "${1}" "${3}" | sort)
elif [ "${2}" ]; then
    ag --color --group --filename "${1}" $(ag -l "${1}" "${2}" | sort)
else
    echo "Requires 2 or 3 args." 1>&2
    exit 1
fi

